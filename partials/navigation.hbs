<div class="nav">
    <h3 class="nav-title">Menu</h3>
    <a href="#" class="nav-close">
        <span class="hidden">Close</span>
    </a>
    <ul>
        {{#foreach navigation}}
            <li class="nav-{{slug}}{{#if current}} nav-current{{/if}}" role="presentation">
              <a href="{{url absolute="true"}}">{{label}}</a>
            </li>
        {{/foreach}}
    </ul>
    <a class="subscribe-button" target="_blank" href="http://eepurl.com/b8pRT1">Subscribe</a>
</div>
<span class="nav-cover"></span>
<!-- Navigation in screen size over 600px -->
<nav class="horizontal-nav">
  <ul>
    {{#foreach navigation}}
      <li class="nav-{{slug}}{{#if current}} nav-current{{/if}}" role="presentation">
        <a href="{{url absolute="true"}}">{{label}}</a>
      </li>
    {{/foreach}}
    <li class="subscription" role="presentation">
      <a class="subscribe-button" target="_blank" href="http://eepurl.com/b8pRT1">Subscribe</a>
    </li>
  </ul>
</nav>
